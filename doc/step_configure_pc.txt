1. snmp
step1: cài đặt.
apt-get install snmp snmpd snmp-mibs-downloader

step2: bỏ log của snmp, bỏ qua bước này log bắn mấy ngày là tràn emmc :) .
vim /etc/default/snmpd

sửa dòng này thành.

SNMPDOPTS='-Lsd -Lf /dev/null -u snmp -g snmp -I -smux,mteTrigger,mteTriggerConf -p /run/snmpd.pid'

SNMPDOPTS='-Lf /dev/null -u snmp -g snmp -I -smux,mteTrigger,mteTriggerConf -p /run/snmpd.pid'

bỏ option -Lsd đi.

step3: cấu hình snmpd
pop_outdoor_manager/outdoor_master_gateway/snmp/snmp_deploy

step4:
reboot.

2. mqtt
step1: cài service phụ thuộc.

apt-get install -y debhelper libc-ares-dev libssl-dev libwrap0-dev python-all python3-all uthash-dev uuid-dev libuuid1 xsltproc docbook-xsl

make -j4

make install 

3.curl

step1: cài service phụ thuộc.
apt-get cmake automake

cmake .

make -j4

make install.
